on:
  workflow_call:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Git pull
        if: ${{ github.ref == 'refs/heads/main' }}
        run: git pull

      - uses: ./
        with:
          sonarToken: 4476ec3b133087fb5893752b1d1b437d0369787c
